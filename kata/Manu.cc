#include <iostream>
#include <algorithm>
#include <vector>
#include <array>

using namespace std;
using VC = vector<char>;
using IA = array<int, 10>;

int main() {
    IA currList;
    string s;
    cin >> s;
    for (auto& it: s) {
        IA nextList;
        if (currList.empty()) {
        }
    }

}

