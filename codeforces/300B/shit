

    int log(int n) {
        int p = 0;
        while ( n > 0) {
            p++;
            n /= 10;
        }
        return p-1;
    }
    int pow(int n) {
        int res = 1;
        for (int i = 0; i < n; i++) res *= 10;
        return res;
    }

