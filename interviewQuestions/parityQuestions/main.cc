#include <iostream>
#include "ParityQuestions.h"

using namespace std;

ParityFinder pf;
typedef unsigned long long UL;
int main(int argc, char **argv) {
    unsigned long long t = 201;
    //cout << "Ans = " << pf.numberSwapper((UL) 240);
    //pf.optimizedNumberSwapper();
    cout << "Min W = " << pf.div(52, 3);
    return 0;
}
